http:
  port: 8123
  bindAddress: "0.0.0.0"
# token은 기본적으로 비워둡니다. 실제 값은 BRIDGE_TOKEN 환경변수(권장, docker-compose에서
# 설정)로 넘기거나 여기 token 값을 직접 채우세요. 둘 다 비어 있으면 기동 시 임의 토큰을
# 생성해 로그에 1회 경고로 출력합니다(무방비로 열리지 않도록 하는 최소한의 안전장치).
token: ""
